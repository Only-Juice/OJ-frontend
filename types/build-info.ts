// Types for build information
export interface BuildInfo {
    commitHash: string;
    shortCommitHash: string;
    branchName: string;
    commitDate: string;
    buildTime: string;
    buildTimestamp: number;
}